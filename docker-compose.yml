version: '3.3'

services:

  # jira:
  #   image: lehungio/infinity:jira
  #   volumes:
  #       - ./:/code

  jira_test:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "7001:7001"

  web:
    image: nginx:latest
    ports:
      - "80:80"
    volumes:
      - ./site.conf:/etc/nginx/conf.d/site.conf
    depends_on:
      - jira_test
    env_file:
      - .env